import React from 'react'

const notes = () => {
  return (
    <div>
      <p>The virtual dom has a strategy that appeared to solve the modifications or mutations that the DOm suffers when using a web or mobile applications.</p>

      <p>***The virtual DOM (VDOM) is a programming concept where an ideal, or “virtual”, representation of a UI is kept in memory and synced with the “real” DOM by a library such as ReactDOM. This process is called reconciliation.</p>

      <h3>Conditional Rendering</h3>
      <p>In React, conditional rendering refers to the process of delivering elements and components based on certain conditions.</p>

      
    </div>
  )
}

export default notes
